void test(double v1);

int otogdestv(double xscale,double yscale,int t100, int t10s,
			  bool newinstrument,
			  double* ksi, double* eta, double* xn10, double* yn10,int* sootv,
			  bool bio,
			  double tet,
			  double eps
					);

/*int otogdestv(double xscale,double yscale,int t100, int t10s,
           bool newinstrument,
           double* ksi, double* eta, double* xn10, double* yn10,int* sootv,
           bool bio, //Флаг учета ориентировки
           double tet, // ориентировка в градусах
           double eps // неопределенность в ориентировке в градусах
               )
               */
